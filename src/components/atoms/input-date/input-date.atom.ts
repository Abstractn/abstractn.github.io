import { AbsComponent } from 'abs-component';
import { getNode } from 'abs-utilities';

export class InputDate implements AbsComponent {
  constructor(public readonly node: HTMLElement) {

  }

  init() {}
  
  ready() {}
}